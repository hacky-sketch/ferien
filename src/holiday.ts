export type Holiday={
    temperature:number, //in °C
    expectedweathercondition:string; //rain, snow, sunshine, storm
    currentenergylevel: number, // 1-10
    plannedlocation:string,//select
    availabletime:number // In days
}